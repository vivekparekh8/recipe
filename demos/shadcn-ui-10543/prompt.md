Fix shadcn-ui/ui issue #10543 (https://github.com/shadcn-ui/ui/issues/10543): Base UI Select popups are horizontally misaligned with their triggers, particularly Base Luma, because alignItemWithTrigger aligns selected-item text while the popup width remains exactly var(--anchor-width), so horizontal item inset leaves the popup edge short.

Work only in this repository. Read CONTRIBUTING.md and apps/v4/registry/README.md first. Do not commit, push, comment, or open a pull request; Recipe will create the final commit. Keep the change narrowly scoped and preserve generated-file conventions. Investigate the Base UI Select positioning and style padding rather than blindly applying the issue comment. Implement the smallest robust fix in the authored Base Select registry source, ensure every generated Base style remains consistent through the registry build, and do not modify Radix styles or lockfiles.

Add a focused regression check where practical that demonstrates popup sizing compensates for item-alignment inset while preserving the alignItemWithTrigger=false behavior. Run the smallest authoritative check first, then the relevant targeted registry build, typecheck, lint, and formatting checks available for the touched scope. Review the final diff and leave the worktree with only the focused source/test/generated changes required by the fix. In your final response, summarize the root cause, changed files, and exact commands/results. Do not create a git commit.
